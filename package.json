{
  "name": "salenlo-workspace",
  "private": true,
  "description": "Convenience scripts for SALENLO monorepo (optional)",
  "version": "0.0.1",
  "scripts": {
    "tunnel": "cloudflared tunnel --url http://localhost:8000",
    "prepare": "husky",
    "bump-version": "node scripts/bump-version.js"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
